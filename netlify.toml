[functions]
  # Extend timeout to 26 s (maximum on free/pro Netlify plans).
  # The fetch-vacancies scraper runs 3 parallel HTTP requests and needs
  # more than the default 10 s to complete reliably.
  included_files = ["netlify/functions/**"]

[functions."fetch-vacancies"]
  timeout = 26
