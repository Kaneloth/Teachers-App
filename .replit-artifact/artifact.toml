kind = "web"
previewPath = "/"
title = "EduCross"
version = "1.0.0"
id = "artifacts/educross"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25023

[services.development]
run = "pnpm --filter @workspace/educross run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/educross", "run", "build" ]
publicDir = "artifacts/educross/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25023"
BASE_PATH = "/"
